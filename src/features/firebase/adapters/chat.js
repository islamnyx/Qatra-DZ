/**
 * TODO (Firebase team): optional Cloud Function for AI, or store chat in chat_sessions.
 * Chat UI team owns src/features/chat/ — you provide data via these functions.
 */
export async function sendChat(_message, _lang, _donorId) {
  return undefined;
}

export async function getChatPrompts(_lang) {
  return undefined;
}
