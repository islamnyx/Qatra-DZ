/**
 * TODO (Firebase team): wilayas collection — map team may also write geolocation fields here.
 * Coordinate with map team on schema: { name, nameAr, status, shortage, hospitals, lat?, lng? }
 */
export async function getWilayas() {
  return undefined;
}
