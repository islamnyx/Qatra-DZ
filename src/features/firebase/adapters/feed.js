/**
 * TODO (Firebase team): news_feed + increment campaign_interest with runTransaction.
 */
export async function getFeed(_donorId) {
  return undefined;
}

export async function registerCampaign(_feedId, _donorId) {
  return undefined;
}
